export class IAbout {
    constructor() {
        console.log('IAbout constructor');
    }

    onIAboutEvent(e, ctx) {
        switch (e.type) {
            case 'click':
                switch (e.target.id) {

                    default:
                        break;
                }

            default:
                break;
        }
    }

}